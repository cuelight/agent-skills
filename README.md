# CueLight Agent Skills

This repository contains public Agent Skills for the CueLight product, aligned to the agentskills.io skill format.

- Each root directory is one CueLight-related skill package.
- Each skill package contains a `SKILL.md` with frontmatter (`name`, `description`) and any bundled resources it actually needs.
- The current public skill in this repository is `cuelight-drama`.
